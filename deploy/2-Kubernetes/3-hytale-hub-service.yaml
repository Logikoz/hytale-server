apiVersion: v1
kind: Service
metadata:
  name: hytale-hub
  namespace: hytale
spec:
  type: LoadBalancer
  selector:
    app: hytale
    role: hub
  ports:
  - name: game
    port: 5520
    targetPort: 5520
    protocol: UDP
